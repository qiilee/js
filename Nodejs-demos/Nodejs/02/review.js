
function fn() {
	// 逻辑
	for(var i=0; i<10; i++) {

	}
	return v;
}

// ES6 提供另外一种语法格式定义函数

// a) 函数名
// b) 参数
// c) 返回值

// fn = v => v; 
// fn 函数名，左侧 v 参数，右侧 v 返回值

// 没有参数
// fn = () => v
// fn 函数名，没有参数使用()，右侧 v 返回值

// 函数体有逻辑
fn = (v, f) => {
	// 在花括号中定义函数体
}

console.log(typeof x);

// let x;

// console.log(x);

