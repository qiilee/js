1、通过01 02 03 04 引出angular

2、01 字符串拼接，代码凌乱不堪，更新困难（比如说更换展示数据的方式）

3、02 引入了模板引擎，操作简单方便了跟多，但是缺少数据交互的特征

4、03 从模板的基础上添加了代码的显示逻辑，提出将代码分离的结构，分为 1模板控制如何显示数据
   2数据控制数据是什么样子，3中间协调的是控制器，是桥梁将模板和数据融合为html，在显示到页面上

5、对两次传送图片的解释，客户端首次获得的是html模板，可请求数据的代码结构（ajax）
   控制器的代码，当客户端解析解析html和js代码的时候，ajax请求数据，返回JSON字符串
   ,JSON.parse()一下，最后将模板和对象合并，生成的是html代码，再渲染

6、04-website是把之前的固定的数据分离出来，通过ajax请求获得